# 📋 Weekly Tasks API

Express.js приложение для управления задачами на неделю с REST API.

## 📋 Описание

Полнофункциональное Express.js приложение, которое демонстрирует:
- Создание базового Express сервера
- Построение REST API с операциями CRUD
- Работу с параметрами (params, query)
- Обработку тела запроса (JSON, URL-encoded)
- Собственные middleware (logger, validator, error handler)
- Раздачу статических файлов
- Модульную архитектуру (routes, controllers, models)

## 🚀 Быстрый старт

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/Mikle-kekich/KR-Industr2.git
cd weekly-tasks-api

# Установка зависимостей
npm install
```

### Запуск

```bash
# Запуск в режиме разработки
npm run dev

# Запуск в режиме production
npm start
```

Сервер запустится на `http://localhost:3000`

## 📚 API Endpoints

### Получение задач

| Метод | Endpoint | Описание |
|-------|----------|---------|
| GET | `/api/tasks` | Получить все задачи |
| GET | `/api/tasks/:id` | Получить задачу по ID |
| GET | `/api/tasks/day/:day` | Получить задачи на день |
| POST | `/api/tasks` | Создать новую задачу |
| PUT | `/api/tasks/:id` | Обновить задачу |
| DELETE | `/api/tasks/:id` | Удалить задачу |

### Параметры запроса

- `day` - день недели (пн, вт, ср, чт, пт, сб, вс)
- `priority` - приоритет (low, medium, high)
- `confirm` - подтверждение для операций удаления

### Примеры запросов

#### Получить все задачи
```bash
curl http://localhost:3000/api/tasks
```

#### Получить задачи на понедельник
```bash
curl http://localhost:3000/api/tasks?day=пн
```

#### Получить задачи с высоким приоритетом
```bash
curl http://localhost:3000/api/tasks?priority=high
```

#### Создать новую задачу
```bash
curl -X POST http://localhost:3000/api/tasks \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Подготовить отчёт",
    "description": "Квартальный отчёт",
    "day": "пн",
    "priority": "high"
  }'
```

#### Обновить задачу
```bash
curl -X PUT http://localhost:3000/api/tasks/1 \
  -H "Content-Type: application/json" \
  -d '{
    "completed": true,
    "priority": "medium"
  }'
```

#### Удалить задачу
```bash
curl -X DELETE http://localhost:3000/api/tasks/1
```

## 📁 Структура проекта

```
weekly-tasks-api/
├── bin/
│   └── www                 # Точка входа приложения
├── controllers/
│   └── taskController.js   # Логика обработки задач
├── routes/
│   ├── index.js           # Основные маршруты
│   └── api/
│       └── tasks.js       # API маршруты
├── middleware/
│   ├── logger.js          # Логирование запросов
│   ├── validator.js       # Валидация данных
│   └── errorHandler.js    # Обработка ошибок
├── models/
│   └── Task.js            # Модель задачи
├── public/
│   ├── index.html
│   ├── css/
│   │   └── style.css
│   └── js/
│       └── app.js
├── app.js                 # Основное приложение
├── package.json
├── .gitignore
└── README.md
```

## ⚙️ Технологии

- **Express.js** - веб фреймворк
- **body-parser** - парсинг тела запроса
- **cors** - поддержка кросс-доменных запросов
- **morgan** - логирование HTTP запросов
- **nodemon** - перезагрузка при изменении файлов (dev)

## 🧪 Тестирование

Используйте REST Client расширение в VS Code или создайте файл `requests.http`:

### Примеры запросов

```http
### Получить все задачи
GET http://localhost:3000/api/tasks

### Создать задачу
POST http://localhost:3000/api/tasks
Content-Type: application/json

{
  "title": "Новая задача",
  "day": "пн",
  "priority": "high"
}

### Обновить задачу
PUT http://localhost:3000/api/tasks/1
Content-Type: application/json

{
  "completed": true
}

### Удалить задачу
DELETE http://localhost:3000/api/tasks/1
```

## ✨ Ключевые особенности

✅ **Собственные Middleware**
- `logger.js` - логирование всех HTTP запросов
- `validator.js` - валидация данных перед обработкой
- `errorHandler.js` - централизованная обработка ошибок

✅ **REST API**
- GET, POST, PUT, DELETE операции
- Параметры маршрута (params)
- Query параметры
- Обработка JSON и URL-encoded данных

✅ **Модульная архитектура**
- Routes - определение маршрутов
- Controllers - логика обработки
- Models - структуры данных
- Middleware - обработка запросов

<img width="2239" height="1178" alt="image" src="https://github.com/user-attachments/assets/575e5698-a69e-44b5-8438-fe88865d51f8" />
<img width="2239" height="1196" alt="image" src="https://github.com/user-attachments/assets/296d69f5-0e3d-4043-b6f6-9e1f2dcf102a" />




